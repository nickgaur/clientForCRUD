import React from "react";
import Form from "./Form";
import './Card.css'

const Card = (props) => {
    return (
        <div className="card">
            <h1>User Registration Form</h1>
            <Form />
        </div>)
}

export default Card;